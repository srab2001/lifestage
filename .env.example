# Postgres (Neon). Omit locally to fall back to an in-memory store.
DATABASE_URL=

# Google OAuth — gates the /dashboard staff surface only. Not the
# Veteran-facing identity provider (see auth.ts). Restricted server-side to
# @adhocteam.us accounts.
AUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
